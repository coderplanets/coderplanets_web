/*
 *
 * ArrowButtons
 *
 */

import { FC } from 'react'

import type { TSize } from '@/spec'

type TProps = {
  // children?: any
  // onClick?: () => void
  size?: TSize
  // direction?: 'left' | 'right'
  // dimWhenIdle?: boolean
  // disabled?: boolean
  // arrowStyle?: 'default' | 'simple'
}

const ArrowButton: FC<TProps> = () => <div>i</div>
export default ArrowButton
