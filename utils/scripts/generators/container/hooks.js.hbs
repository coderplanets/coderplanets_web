//

/*
 *
 * {{ properCase name }}
 *
 */

import React from 'react'

import { connectStore, buildLog } from '@/utils'
{{#if wantMessages}}
  import { useTrans } from '@/hooks'
{{/if}}

import { Wrapper } from './styles'
import { useInit } from './logic'

/* eslint-disable-next-line */
const log = buildLog('C:{{ properCase name }}')

const {{ properCase name }}Container = ({{preCurly ""}} {{ camelCase name}} {{afterCurly ""}}) => {
  useInit({{ camelCase name }})

  {{#if wantMessages}}
  const { t } = useTrans()
  {{/if}}

  return (
    <Wrapper testid="{{ camelCase name}}">
      <h2>{{ properCase name }} hooks container!</h2>
      <div>impress me!</div>
    </Wrapper>
  )
}

export default connectStore({{ properCase name }}Container)
