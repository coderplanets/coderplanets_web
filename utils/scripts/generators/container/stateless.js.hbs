 /*
 *
 * {{ properCase name }}
 *
 */

import React from 'react'

// import styled from 'styled-components'
{{#if wantI18n}}
import { FormattedMessage as I18n } from 'react-intl'
import lang from './lang'
{{/if}}

import observer from '../../utils/mobx-utils'

const selector = ({ store }) => ({
  xx: store.xx,
})

const {{ properCase name }} = observer(selector, ({ _, xx }) => {
  return (
    <div>
      {{ properCase name }} container is here!
      {{#if wantI18n}}
        <I18n {...lang.header} />
      {{/if}}
    </div>
  )
})

export default {{ properCase name }}
